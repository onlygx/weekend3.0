<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="SettingsMapper" >

  <cache type="org.mybatis.caches.ehcache.LoggingEhcache"/>

  <select id="selectByPrimaryKey" resultType="settings" parameterType="settings" >
    select 
    id, `name`, `value`, account_id, `type`
    from t_settings
    where id = #{id,jdbcType=BIGINT}
  </select>
  <delete id="deleteByPrimaryKey" parameterType="java.lang.Long" >
    delete from t_settings
    where id = #{id,jdbcType=BIGINT}
  </delete>
  <insert id="insert" parameterType="settings" >
    insert into t_settings (id, `name`, `value`, 
      account_id, `type`)
    values (#{id,jdbcType=BIGINT}, #{name,jdbcType=VARCHAR}, #{value,jdbcType=VARCHAR}, 
      #{accountId,jdbcType=BIGINT}, #{type,jdbcType=INTEGER})
  </insert>

  <update id="updateByPrimaryKey" parameterType="settings" >
    update t_settings
    <set >
      <if test="name != null" >
        `name` = #{name,jdbcType=VARCHAR},
      </if>
      <if test="value != null" >
        `value` = #{value,jdbcType=VARCHAR},
      </if>
      <if test="accountId != null" >
        account_id = #{accountId,jdbcType=BIGINT},
      </if>
      <if test="type != null" >
        `type` = #{type,jdbcType=INTEGER},
      </if>
    </set>
    where id = #{id,jdbcType=BIGINT}
  </update>

  <select id="list" resultType="settings" parameterType="pd">
    select * from t_settings
  </select>
</mapper>